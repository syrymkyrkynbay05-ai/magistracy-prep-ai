services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    restart: always

  frontend:
    build: .
    ports:
      - "5173:80"
    depends_on:
      - backend
    restart: always
