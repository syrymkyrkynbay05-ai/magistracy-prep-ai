[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python backend/seed_db.py && uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/"
healthcheckTimeout = 600
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
